version: 1
frontend:
  phases:
    preBuild:
      commands:
        - 'yarn install --immutable'
    build:
      commands:
        - 'yarn run build'
  artifacts:
    baseDirectory: apps/seller/dist
    files:
      - '**/*'
  cache:
    paths:
      - 'node_modules/**/*'
